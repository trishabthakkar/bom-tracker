# Read by `docker compose` (via npm run db:up) for local Postgres port mapping.
# Copy to .env if the default port 5432 is already in use on your machine.
# If you change this, also update the port in DATABASE_URL inside backend/.env.
POSTGRES_PORT=5432
